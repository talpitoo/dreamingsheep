.gitkeep
.env*
*.ico
*.lock
db/migrations
.next
# regenerated by every `next dev`/`next build` with a trailing semicolon; prettier
# (semi: false) stripped it back on each commit, so the file was never not dirty
next-env.d.ts
.yarn
.pnp*
